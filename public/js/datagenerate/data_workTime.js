const CONST = require('./Constant');

const data_workTime = {
    /**
     * 작업시간을 반환한다.
     * @param {Object} userInput 사용자 입력 
     * @param {String[]} callingPorts 기항지 배열
     */
    getWorkTime(userInput, callingPorts) {
        let workTime = 10000;
        return workTime;
    }
}

module.exports = data_workTime;
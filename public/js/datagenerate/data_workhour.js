const CONST = require('./Constant');

const data_workhour = {
    /**
     * 운항시간을 반환한다.
     * @param {Object} userInput 사용자 입력 
     * @param {String[]} callingPorts 기항지 배열
     */
    getWorkhour(userInput, callingPorts) {
        let totalWorkhour = 10000;
        return totalWorkhour;
    }
}

module.exports = data_workhour;